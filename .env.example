# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real secrets.

# Required. No default anywhere in the app (fails fast if unset) because
# this key signs every JWT the payment-api issues. Generate a fresh one
# with:
#   openssl rand -base64 32
ATLAS_JWT_SECRET=
